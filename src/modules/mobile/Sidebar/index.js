import React from 'react'
import PropTypes from 'prop-types'
import { Grid, Typography, Paper, Drawer } from '@mui/material'

const Sidebar = ({ open, onClose }) => {

    return (
        <></>
    )
}
